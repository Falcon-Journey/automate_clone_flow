# Server
PORT=5001

# Anima (dev.animaapp.com) – required when login is needed
ANIMA_EMAIL=akkisinghal50+5@gmail.com
ANIMA_PASSWORD=Test123@

# Playwright: in Docker always headless. Locally default is headful (visible browser).
# Set PLAYWRIGHT_HEADED=0 to run headless when not in Docker.
# PLAYWRIGHT_HEADED=0

# Optional: override Playwright browsers path (macOS default set in code; Linux: ~/.cache/ms-playwright)
# PLAYWRIGHT_BROWSERS_PATH=/path/to/browsers
